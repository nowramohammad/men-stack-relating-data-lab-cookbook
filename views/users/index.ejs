<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Community Users</title>
</head>
<body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Explore Our Community</h1>
    <ul> 
        <% users.forEach(user => { %>
            <li>
              <a href="/users/<%= user._id %>"><%= user.username || user.email %>'s Pantry</a>
            </li>
          <% }) %>
    </ul>
<% users.forEach(user => { %>
<li>
    <a herf="/users/<%= user._id %>"<%= user.username || user.email %>'s Pantry</a>

</li>
<% }) %>

</ul>
<% if (users.length === 0) { %>
    <p>No users found.</p>
  <% } %>
</body>
</html>